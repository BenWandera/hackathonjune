# How I contributed

I added simple unit tests and updated project requirements to demonstrate participation in the project.

Files added:

- `tests/test_code.py`: tests for `calculate_area` in `code.py`.
- `tests/test_code_analyser.py`: basic test to validate `analyze_file` output structure.
- `requirements.txt`: updated to include `pytest`.

How to run the tests locally:

```bash
python -m pip install -r requirements.txt
python -m pytest -q
```

Suggested commit message:

```
Add pytest, unit tests for code.py and code_analyser, and CONTRIBUTING note
```
